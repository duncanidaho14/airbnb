{% extends "base.html.twig" %}
{% block title %}
    Editer votre annonce
{% endblock %}

{% form_theme form 'annonce/_collection.html.twig' %}

{% block body %}
    <div class="container">
        <h1>Editer votre annonce : {{annonce.title}}</h1>
        {{ form_start(form) }}
        {{ form_widget(form)}}
        <button type="submit" class="btn btn-primary">Editer votre annonce</button>
        {{ form_end(form) }}
    </div>
{% endblock %}




{% block javascripts %}
{{ parent() }}
    <script src="{{asset('build/js/annonce.js')}}"></script>
{% endblock %}